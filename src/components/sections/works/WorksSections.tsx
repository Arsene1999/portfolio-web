import { WorksSectionsTitle, WorksSectionsWrapper } from "./styles";

const WorksSections = () => {
  return (
    <WorksSectionsWrapper id="my-works">
      <WorksSectionsTitle>My Works</WorksSectionsTitle>
    </WorksSectionsWrapper>
  );
};

export default WorksSections;
